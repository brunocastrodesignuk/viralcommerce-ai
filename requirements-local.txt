# ── Dependências mínimas para rodar localmente (sem Docker, sem ML pesado) ───
# Python 3.12+ recomendado. Compatível com 3.14.

# Web Framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.9.2
pydantic-settings==2.6.1
python-multipart==0.0.12

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Database (SQLite local / PostgreSQL produção)
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0          # SQLite async — modo local
asyncpg==0.30.0            # PostgreSQL async — modo produção
alembic==1.14.0

# Cache / Task Queue (modo memória local)
fakeredis[aioredis]==2.26.1
celery==5.4.0

# HTTP
httpx==0.27.2
aiohttp==3.11.7
tenacity==9.0.0

# AI
anthropic==0.40.0

# Data
numpy==2.1.3

# Monitoring
prometheus-client==0.21.0
structlog==24.4.0
python-dotenv==1.0.1
python-slugify==8.0.4
